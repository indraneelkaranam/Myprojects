import { Filter.Pipe.TsPipe } from './filter.pipe.ts.pipe';

describe('Filter.Pipe.TsPipe', () => {
  it('create an instance', () => {
    const pipe = new Filter.Pipe.TsPipe();
    expect(pipe).toBeTruthy();
  });
});
